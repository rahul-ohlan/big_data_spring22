#!/usr/bin/python

import sys
import numpy as np


c1 = sys.argv[1]
c2 = sys.argv[2]

print(c1)
print(c2)















#new_centroids = list()
#for line in sys.stdin:
#    line = line.strip()
#    line = line.split("_")
#    line = np.array(line).astype(np.float64)
#    new_centroids.append(line)

#new_centroids = np.array(new_centroids)

#c1 = np.array(sys.argv[1].split()).astype(np.float64)
#c2 = np.array(sys.argv[2].split()).astype(np.float64)
#c3 = np.array(sys.argv[3].split()).astype(np.float64)
#c4 = np.array(sys.argv[4].split()).astype(np.float64)

#prev_centroids = np.array([c1,c2,c3,c4])

#if (prev_centroids == new_centroids).all():
#    print("Match Found")


#else:
#    print("Not Found")
